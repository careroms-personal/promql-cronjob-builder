# Config file is relative path to pipeline parent dir
config_file_path: "./"

pipelines:
  - id: cpu_query_120_days_back
    metadata:
      cluster_name: REQUEST
      environment: REQUEST
    promql_config: query_range_1
    output_config: output_1
    
    server_configs:
      - server_1
    range_configs:
      - range_1