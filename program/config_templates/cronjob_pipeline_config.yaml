cronjob_pipeline_export_file: "./query_pipeline.yaml"

config_files:
  file_path: "./" # Relative to pipeline config file
  server_config: "server_config.yaml"
  promql_config: "promql_config.yaml"
  range_config: "range_config.yaml"

server_configs:
  - server_1

pipelines:
  - id: cpu_query_120_days_back
    metadata:
      cluster_name: REQUEST
      environment: REQUEST
    promql_config_id: query_range_1
    range_config_id: range_1